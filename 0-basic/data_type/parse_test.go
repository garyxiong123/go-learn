package unitTest

import (
	"testing"
)

//一种是 uint8 类型，或者叫 byte 型，代表了 ASCII 码的一个字符。
//byte 类型是 uint8 的别名，对于只占用 1 个字节的传统 ASCII 编码的字符来说，完全没有问题，例如 var ch byte = 'A'，字符使用单引号括起来。

func Test_Parse(t *testing.T) {

	//txInfo, _ := types.ParseTransferNftTxInfo("ss")
	//println(txInfo)
}
